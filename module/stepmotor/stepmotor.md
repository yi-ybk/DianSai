# Stepmotor 步进电机模块说明

## 当前状态

`module/stepmotor` 目录当前没有源文件或头文件，也没有可调用的步进电机接口。

后续实现前需要明确驱动方式：

- `STEP/DIR/ENABLE` 脉冲驱动器；
- 四相或五相线圈时序驱动；
- 是否需要加减速曲线、位置闭环、限位开关和归零流程。

建议模块对外提供初始化、使能、停机、速度设置、相对/绝对位置控制和周期更新接口，并把高频脉冲生成放在定时器中断或 DMA 中，而不是 FreeRTOS 普通任务的忙等待循环中。

在头文件和实现加入工程前，本目录不能作为可用模块引用。
