# 简单 Float 通信协议

## 1. 串口资源

本协议由 `application/slaver` 模块实现，使用 `UART_SLAVER`：

| 项目 | 配置 |
| --- | --- |
| UART 外设 | UART3 |
| MCU 发送引脚 | PB12（TX） |
| MCU 接收引脚 | PB13（RX） |
| 波特率 | 115200 bps |
| 接收方式 | DMA + 接收超时中断 |
| DMA 通道 | 通道 1 |

与 IMU 串口不冲突。IMU 使用 `UART_IMU0`（UART2、PB15/TX、PB16/RX、460800 bps、DMA 通道 0）。两者的 UART 外设、引脚和 DMA 通道均不同。

接线时应交叉连接并共地：外部设备 TX 接 PB13，外部设备 RX 接 PB12，GND 与开发板 GND 相连。

## 2. 帧格式

每帧固定为 7 字节：

```text
字节序号  0       1~4                 5        6
内容      帧头    float（IEEE 754，小端） 校验和   帧尾
```

默认帧头为 `0xA5`，默认帧尾为 `0x5A`。

`float` 必须是单精度 IEEE 754 浮点数，并按小端序传输：最低有效字节在前。

校验和是字节 0 至字节 4 的无符号 8 位累加和，计算结果只保留低 8 位：

```text
checksum = (header + data[0] + data[1] + data[2] + data[3]) & 0xFF
```

校验和不包含自身和帧尾。

## 3. 示例

发送 `1.0f` 时，其 IEEE 754 表示为 `0x3F800000`，小端序数据为 `00 00 80 3F`。

```text
A5 00 00 80 3F 64 5A
```

其中：

- 帧头：`A5`
- `float`：`00 00 80 3F`
- 校验和：`A5 + 00 + 00 + 80 + 3F = 0x164`，取低 8 位为 `64`
- 帧尾：`5A`

## 4. 接收行为

只有帧头、帧尾和校验和都正确的帧会被接收。接收有效帧后，模块只保存最新一帧，不建立应用层帧队列；新帧会覆盖旧帧。

在其他任务中读取最新数据：

```c
SlaverSimpleFloatData_t latest;

SlaverSimpleFloatGetLatest(&slaver_float_protocol, &latest);
if (latest.valid)
{
    float value = latest.value;
    /* 使用 value */
}
```

`latest.update_count` 是成功接收的有效帧计数，`latest.last_update_tick` 为最近一次更新时的系统节拍。

## 5. 发送数据

通过下列函数发送一帧：

```c
bool ok = SlaverSimpleFloatSend(&slaver_float,
                                &slaver_float_protocol,
                                12.5f);
```

函数会自动生成小端序 float、校验和及帧尾。返回 `false` 表示串口未初始化、参数无效或发送超时。

## 6. 修改帧头或帧尾

如需调整帧头、帧尾或帧长度，在 `application/robot.c` 的 `slaverInit()` 中修改 `protocol_config`；当前 float 协议的数据布局固定，因此帧长度必须为 7：

```c
const SlaverSimpleFloatProtocolConfig_t protocol_config = {
    .frame_header = 0xA5U,
    .frame_tail = 0x5AU,
    .frame_length = 7U,
};
```

`SlaverInitConfig_t.header` 同样应填写与 `protocol_config.frame_header` 相同的帧头，用于底层帧同步。
